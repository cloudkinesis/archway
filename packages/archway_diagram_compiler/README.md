# Archway Diagram Compiler

Deterministic AWS architecture diagram compiler that accepts a semantic architecture spec and produces D2, rendered artifacts, and QA diagnostics.

This package is intentionally independent of Archway. The only Archway-specific code lives in `archway_diagram_compiler.adapters.archway`.
